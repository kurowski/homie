[user]
name  = "Scout Homes"
email = "scout@homie.sh"

[packages]
all    = ["git"]
feodra = ["typo"]                  # typo for "fedora"

[packages."tag:"]                  # empty tag name
fedora = ["unreachable"]

[packages."tag:work"]
fedora    = ["kubectl"]
ubunntu   = ["terraform"]          # typo for "ubuntu"
